# Release v0.1.0

Initial minimal release providing:
- TypeScript Express backend
- Onboarding plan generator (rule-based + mock LLM)
- Unit tests and CI workflow
- Dockerfile and docker-compose for local dev
